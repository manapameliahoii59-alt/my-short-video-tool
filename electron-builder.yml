appId: com.zs.assistant
productName: comedyapplication

# 🌟 全局发布配置 (用于生成 latest.yml)
publish:
  provider: generic
  url: "http://129.204.86.63:3535/updates"

directories:
  buildResources: build
  output: dist

files:
  - "!**/.vscode/*"
  - "!src/*"
  - "!electron.vite.config.{js,ts,mjs,cjs}"
  - "!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}"
  - "!{.env,.env.*,.npmrc,pnpm-lock.yaml}"

asarUnpack:
  - resources/**

win:
  executableName: comedyapplication
  # 🌟 Windows 平台特定发布配置 (解决你刚才的报错)
  publish:
    provider: generic
    url: "http://129.204.86.63:3535/updates"

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  deleteAppDataOnUninstall: false
  allowElevation: true
  installerIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
  installerHeaderIcon: build/icon.ico
  createDesktopShortcut: always
  createStartMenuShortcut: true
  shortcutName: "漫剧助手"
  uninstallDisplayName: "卸载 漫剧助手"
  runAfterFinish: true
  # 生成的文件名格式：comedyapplication-1.0.1-setup.exe
  artifactName: ${productName}-${version}-setup.${ext}

mac:
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
  artifactName: ${productName}-${version}.${ext}

dmg:
  artifactName: ${productName}-${version}.${ext}

linux:
  target:
    - AppImage
    - deb
  maintainer: electronjs.org
  category: Utility
  artifactName: ${productName}-${version}.${ext}

npmRebuild: false
electronDownload:
  mirror: https://npmmirror.com/mirrors/electron/
